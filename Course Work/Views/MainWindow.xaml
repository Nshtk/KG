<Window x:Class="KP.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Local="clr-namespace:KP.ViewModels" 
        mc:Ignorable="d"
        Title="Cryptography" Height="500" Width="600">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Button Grid.Row="0" Grid.Column="0" Content="Client side" Command="{Binding CommandSwitchView}" CommandParameter="{x:Static Local:ViewModelMain+ViemodelName.CLIENT}"></Button>
        <Button Grid.Row="0" Grid.Column="2" Content="Server side" Command="{Binding CommandSwitchView}" CommandParameter="{x:Static Local:ViewModelMain+ViemodelName.SERVER}"></Button>
        
        <ContentControl Grid.Row="0" Grid.Column="1" Content="{Binding CurrentViewmodel}"></ContentControl>
    </Grid>
</Window>
